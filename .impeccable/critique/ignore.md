# Critique findings to not re-raise

# Space Grotesk is a deliberate brand reuse from the SquawkIt app (wingslog),
# not a default. Identity-preservation across the portfolio is intentional.
overused-font: Space Grotesk
# Roboto only appears as a fallback in the body font stack, never primary.
overused-font: roboto
# Card thumbnails are full-bleed images by design; flush edges are intentional.
cramped-padding
